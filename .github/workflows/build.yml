# Build and publish the Macula base images.
#
# ⚠ THE SCHEDULE IS THE POINT, not the push trigger. A base image's job is to
# carry current OS security updates. Built only on change, it rots quietly:
# nobody edits a Containerfile for months while Debian ships CVE fixes nobody
# picks up. The weekly rebuild is what stops that, and it is the reason this
# repo is worth having at all rather than a Containerfile in a service repo.
name: build base images

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'LICENSE'
  schedule:
    # DAILY, 04:00 UTC. Weekly was the first instinct and daily is better, for
    # a reason that is not "more is better":
    #
    #   - it costs nothing. This repo is public, so hosted minutes are free and
    #     unlimited, and a build takes about 60 seconds.
    #   - Debian ships security updates continuously, not weekly. A weekly
    #     rebuild means carrying a known CVE for up to six days longer than
    #     necessary, for no saving.
    #   - it does NOT churn consumers daily. The layers are content-addressed
    #     and the build is cached, so a day where nothing changed upstream
    #     reproduces the same digest and nobody re-pulls. A consumer only sees
    #     a new image when an installed package actually moved, which is
    #     exactly when it should.
    - cron: '0 4 * * *'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    # GitHub-hosted on purpose. This repo is public, so hosted minutes are free
    # and unlimited, and the org's self-hosted runners are (correctly) not
    # offered to public repos. Building base images must not depend on the same
    # machines whose pipelines consume them.
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: macula-ci-pq
            file: Containerfile.ci-pq
          - image: macula-pq-runtime
            file: Containerfile.pq-runtime

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Tags
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/macula-io/${{ matrix.image }}
          # A date tag as well as :latest, so a consumer that needs to pin
          # against a bad rebuild has something to pin TO. Pinning to a digest
          # works too and is stricter.
          tags: |
            type=raw,value=latest
            type=raw,value={{date 'YYYYMMDD'}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ matrix.file }}
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.image }}
          cache-to: type=gha,scope=${{ matrix.image }},mode=max
          provenance: false
